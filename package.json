{
  "name": "callao-misas-vue",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "cp index.template.html index.html && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp dist/index.html index.html && cp dist/404.html 404.html && rm -rf assets && cp -r dist/assets assets && git add . && git commit -m 'Deploy to GitHub Pages' && git push origin main"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.3.5"
  }
}
